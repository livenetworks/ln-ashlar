node_modules/
.DS_Store
*.log
*.sqlite
js/ln-icon/tabler/
/dist/
 
# Compiled standalone components are tracked on purpose: consumers may pick
# individual components via <script src>, without running a build.

# Source material the docs-mcp corpus was written from (drafts, mirrored
# pre-corpus docs). Not served — the MCP indexer skips `_`-prefixed paths.
docs-mcp/_incoming/
